# Demo assets

- `streamlit-*.svg` — Streamlit dashboard stills (GitHub-safe SVG)
- `verify-*.svg` — TruthGraph-style `visioneval verify` stills (supported / contradicted / CLI); PNG masters also kept for the silent demo video
- `verify-*.png` — same stills as PNG (LinkedIn/demo masters; optional in-repo)
